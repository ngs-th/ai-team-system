# 2026-02-05 - Workflow Fixes (Review/Prereq/Truthful Dashboard)

## เป้าหมาย
- ทำให้ workflow ทำงาน “จริง” และแดชบอร์ดสะท้อน session/สถานะจริง
- แยก `blocked` เป็น attribute (ไม่ใช่ derive จาก feedback)
- Prerequisites ไม่ครบ: “ตีกลับ” ได้โดยไม่ตีเป็น blocked (ยกเว้นกรณีต้องรอคนจริง ๆ)

## Changes (สรุป)
- Review Manager:
  - ตรวจ prerequisites ก่อนเริ่ม review เสมอ
  - ถ้าเจอ prerequisites ที่เป็น HUMAN-only (`@human`/`HUMAN:`/`🔒`) → mark task เป็น `info_needed`
  - เอา auto-requeue เรื่อง “missing working memory” ออก (ให้ reviewer ทำงานจริง; approval ยัง gate ที่ `task approve`)
  - ป้องกัน task กระโดดจาก `todo` ไป `review` โดย reconcile เฉพาะ `in_progress`
- Task reject/review loop:
  - `task reject` จะ **ไม่เพิ่ม fix loop** ถ้า reason เป็นเรื่อง prerequisites/working memory (ลด auto-stop ผิด ๆ)
- HUMAN-only prerequisites:
  - เพิ่ม flag ใน checklist item และบังคับ `task check ... --actor human` ถึงจะติ๊กได้
- Dashboard:
  - เวลาใน modal + duration ใช้ Bangkok (+7) แบบสอดคล้องกัน
  - แสดง `info_needed` เป็น blocked attribute และ infer lane จาก history
  - จุดเขียวกระพริบย้ายไปมุมซ้ายบนเพื่อไม่ชนกับป้าย BLOCKED
  - column สูงขึ้นและ scroll ได้
- Runtime artifacts:
  - อัปเดต `.gitignore` เพื่อ ignore logs/assistant artifacts และถอด `logs/audit.log` ออกจาก git tracking

## Notes
- Task ที่ต้องใช้ API key/secret ของจริง ควรตั้ง prerequisite เป็น HUMAN-only เพื่อกัน agent “ติ๊กผ่านเอง”

